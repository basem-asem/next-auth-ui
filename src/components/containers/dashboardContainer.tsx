import React from 'react';

const DashboardContainer = ({ children }: any) => {
    return (
        <div className="py-[10px] px-[10px] sm:py-[30px] sm:px-[30px] w-full m-auto">{children}</div>
    )
}

export default DashboardContainer;
