import React from 'react';

const AuthContainer = ({ children }: any) => {
    return (
        <div className="py-[30px] px-[10px] w-full m-auto max-w-[472px]">{children}</div>
    )
}

export default AuthContainer;
